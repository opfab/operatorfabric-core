dependencies {
    compile cloud.eurekaServer, boot.starterAop, spring.retry
}

jar {
    manifest {
        attributes("Created-By"         : "David Binder",
                "Specification-Title": "Operator Fabric Microservices Registry",
                "Main-Class"         : "org.lfenergy.operatorfabric.registry.Application",
                "Class-Path"         : configurations.compile.collect { it.getName() }.join(' '))
    }
}
