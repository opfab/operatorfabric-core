dependencies {
    compile cloud.eurekaServer, boot.starterAop, spring.retry
}

bootJar {
    manifest {
        attributes("Created-By"          : "Gradle ${gradle.gradleVersion}",
                "Specification-Title"    : "OperatorFabric Microservices Registry",
                "Implementation-Title"   : "OperatorFabric Microservices Registry",
                "Implementation-Version" : operatorfabric.version,
                "Specification-Version"  : operatorfabric.version
        )
    }
}
