version: '2.1'
services:
  rabbitmq:
    container_name: rabbit
    image: rabbitmq:3-management
    ports:
      - "5672:5672"
      - "15672:15672"
      - "15674:15674"
  mongodb:
    container_name: mongodb
    image: mongo:6.0.9-rc0-jammy
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: password
    volumes:
      - "./mongodump:/dump"
  keycloak:
    container_name: keycloak
    image: quay.io/keycloak/keycloak:19.0.3-legacy
    command: -Dkeycloak.migration.action=import -Dkeycloak.migration.provider=dir -Dkeycloak.migration.dir=/keycloak/export -Djboss.http.port=89
    environment:
      - KEYCLOAK_USER=admin
      - KEYCLOAK_PASSWORD=admin
      - DB_VENDOR=H2
    volumes:
      - ../keycloak:/keycloak
    ports:
      - "89:89"
      - "90:9990"
  web-ui:
    container_name: web-ui
    image: "lfeoperatorfabric/of-web-ui:SNAPSHOT"
    ports:
      - "2002:80"
    volumes:
      - "./favicon.ico:/usr/share/nginx/html/favicon.ico"
      - "./ui-config:/usr/share/nginx/html/config"
      - "../../src/test/externalWebAppExample:/usr/share/nginx/html/external/appExample"
      - "./nginx.conf:/etc/nginx/conf.d/default.conf"
  ext-app:
    container_name: external-app
    image: "lfeoperatorfabric/of-external-app:SNAPSHOT"
    ports:
      - "8090:8090"
    volumes:
      - "../certificates:/certificates_to_add"
  mailhog:
    image: mailhog/mailhog:v1.0.1
    ports:
      - 1025:1025
      - 8025:8025

