operatorfabric.businessconfig:
  storage:
    path: "build/test-data/businessconfig-storage"
spring:
  mongodb:
    uri: mongodb://root:password@localhost:27017/?authSource=admin #use for test environment only
    database: operator-fabric-test
  jackson:
    default-property-inclusion: non_null #to avoid overhead of sending null values
